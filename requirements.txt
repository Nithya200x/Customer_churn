# requirements.txt
# Lists all Python libraries needed to run this project
# Install all at once using: pip install -r requirements.txt

pandas          # For loading and working with the dataset
matplotlib      # For creating graphs and charts
scikit-learn    # For machine learning model (Logistic Regression)
seaborn         # For better-looking plots (optional but helpful)
